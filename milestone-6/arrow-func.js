const add = (a, b) => a + b;
console.log(add(1, 2));

const add2 = (a, b) => {
  return a + b;
};
console.log(add(1, 2));

function isPositive(number) {
  return number >= 0;
}

const isPositive = (number) => number >= 0;
